interface:
  display_name: "Handoff"
  short_description: "Compact a conversation into a handoff"
  default_prompt: "Create a handoff for the next session and describe its intended focus."
policy:
  allow_implicit_invocation: false
